# Claude Code Rules for SerializeSubsystem

## Git & GitHub

- **Never create a PR targeting `master` directly.** All PRs must target a proper base branch (e.g., `dev` or another feature branch).
- **Never add Claude as a co-author in commits.** Do not include `Co-Authored-By:` or `Claude-Session:` lines in commit messages.
